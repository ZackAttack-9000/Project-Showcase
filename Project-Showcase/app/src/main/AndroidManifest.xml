<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:enableOnBackInvokedCallback="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProjectShowcase"
        tools:targetApi="tiramisu">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.ProjectShowcase.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.StoryOneInput.StoryOneInputAppCompat"
            android:exported="true"
            android:theme="@style/Theme.ProjectShowcase.NoActionBar" />
        <activity
            android:name=".ui.StoryTwoInput.StoryTwoInput"
            android:exported="true"
            android:theme="@style/Theme.ProjectShowcase.NoActionBar" />
        <activity
            android:name=".ui.StoryThreeInput.StoryThreeInput"
            android:exported="true"
            android:theme="@style/Theme.ProjectShowcase.NoActionBar" />
    </application>

</manifest>